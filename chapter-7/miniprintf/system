mini printf wrapper that handles first argument 
============================================================
E: clone of printf() called miniprintf() 
basically outputs text to standard output
there should be availability to integrate (concatenate) int, char, string variabless inside the text, 
and parameters like: set precision of output digit, 
ConOps: 
using it's own mini syntax to integrate variables into the text and set parameters like precision with it,
and dynamic argument list for passing variables, it will process them and send to standard output


IRD:
void miniprintf(char *txt, ...); dynamic

functionality
 - it shall send string exactly as it is to standard output, excepting variables integration standard
 - it shall integrate variables in the same order stated by syntax inside the text, as the order of arguments

human factor:
 - user shall provide valid string format
 - user shall follow variable integration standard 

performance
 - it should have time complexity of o(n)
 

 DCM:

output sender: 
E: sends string to standard output
IRD: void printf(const char* s);
FRD:
- shall send string exactly as it is as to std output  

integrator:
E: replaces syntax with approprite argument into stated in text place, following the order


 

